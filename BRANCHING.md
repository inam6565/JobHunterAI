# Branching Rule

Every new feature goes on a new branch.

## Workflow

1. Branch from `main`
2. Do the work on the feature branch
3. Open a pull request
4. Merge back into `main`

## Examples

- `feature/postgres-integration`
- `feature/api-docs`
- `fix/jobs-route-validation`
- `chore/ci-hardening`
